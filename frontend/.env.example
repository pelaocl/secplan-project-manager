# URL base de la API del backend
VITE_API_BASE_URL="/api" # Use proxy in vite.config.ts for development
# OR use full URL if backend is on different port/domain and CORS is configured
# VITE_API_BASE_URL="http://localhost:5000/api"